{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/public/(.*)",
      "dest": "https://cdn.readwith.cloud/public/$1"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}